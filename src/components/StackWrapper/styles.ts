import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  goBackButton: {
    paddingLeft: 16,
  },
  backButton: {
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  backView: {
    marginLeft: 10,
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
